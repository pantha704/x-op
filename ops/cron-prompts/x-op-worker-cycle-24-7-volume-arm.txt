One worker cycle for the X op (@your_handle) reply rig on this VPS. FIRST read /home/ubuntu/x-op/ops/WORKER-SPEC.md and /home/ubuntu/x-op/worker/state.json and follow them exactly. Working dir /home/ubuntu/x-op.

STEP 0a (PIPELINE RESUME - do this FIRST): if state.json.pending_verify is set, the previous wave is still posting or just finished. Wait for it (poll `pgrep -f fire_driver.py` every 20s, up to 15 min). Then run scroll_verify + vault_log on its fire log, check the log for a THROTTLED outcome (if present: set throttle_until = now+15m and SKIP this cycle's fire), then clear pending_verify in state.json. If the wave is still running after 15 min: leave pending_verify set, skip this cycle's fire, end the cycle.

GUARDS - if ANY fail: update state.json last_cycle/last_cycle_result with the skip reason and STOP (fire nothing): (1) state.json throttle_until in the future; (2) outside active window 06:30-23:30 UTC UNLESS state.json.window_exception covers now; (3) another HARVEST is already running (pgrep -f harvest_run.py) - a live fire_driver.py does NOT block you (harvest runs on its own tab; only the FIRE launch is single-flight via flock). NO quota guards in max-throughput mode.

Otherwise run ONE cycle (full detail in the spec's Cycle section - follow it exactly):

1. PREP FIRST: check worker/prep-batch.json (or the newest targets/worker-HHMM.json built ahead by a previous cycle). If fresh (prep_ts < 25 min), re-validate it the same way select_latest.py does (dedupe against fire logs 24h + ledger + pending files; drop anything older than 60 min at now) and use it as this cycle's batch - top up with a harvest only if fewer than 8 survive. If missing/stale: harvest live via `./venv/bin/python harvest_run.py <your 16-20 lanes>`, select per the engagement doctrine with worker/select_latest.py (live window priority; if thin - never idle, never pad with graveyard fires).
2. COMPOSE WIDE (spec step 3): 5 candidates per target, kill the 4 safest (screenshot test), enforce 45-110 chars, lowercase, one idea, no dashes, no approval tails, no self-diminishment, fact-gate (skip anything you cannot verify from the post itself). QC hard - cut the merely fine; run `./venv/bin/python qc_botcheck.py <batch>` and fix every HARD flag. Also spec step 2c (conversation pass, up to 3) and 2d (quote lane - drafts only, never fire quotes).
3. FIRE DETACHED: `bash /home/ubuntu/x-op/run_wave.sh --detach <your-targets.json> --max 20 --gap-min 15 --gap-max 35`. It returns a pid immediately - do NOT wait for or poll the wave. On REFUSED (flock held) wait 60s and retry, up to 5 min. After a launch that printed `detached pid=`: write state.json pending_verify=<the wave's fire-log path under logs/>, and delete worker/prep-batch.json if it was the consumed batch.
4. PREP PASS (spec step 5c - do NOT end the cycle yet): while the wave fires, harvest fresh + compose-wide + QC up to 20 targets and write them ATOMICALLY to worker/prep-batch.json (write a tmp file then mv; never a partial file). If anything fails, leave it absent - the next cycle falls back to the hot path. Then write state.json last_cycle/last_cycle_result and END the cycle.

Never: fire from stale lists, retry restricted posts, reply to a handle already replied to today (check the ledger first), or force graveyard-family replies. Keep output silent - this job delivers locally.