One worker cycle for the X op (@your_handle) reply rig on this VPS. FIRST read /home/ubuntu/x-op/ops/WORKER-SPEC.md and /home/ubuntu/x-op/worker/state.json and follow them exactly. Working dir /home/ubuntu/x-op. GUARDS - if ANY fail: update state.json last_cycle/last_cycle_result with the skip reason and STOP (fire nothing): (1) state.json throttle_until in the future; (2) outside active window UNLESS state.json.window_exception covers now - the 2026-09-22 exception is IN EFFECT (owner waived the dark window today), so run; (3) pgrep shows any of fire_driver.py / run_wave.sh / chain_resume.sh / harvest_run.py running; (4) today's hits (count outcome=hit lines in logs/fire-$(date -u +%Y%m%d)*.jsonl) >= daily_floor 900; (5) hits in the trailing 75 min >= soft_window 140. Otherwise run ONE cycle: harvest live via /home/ubuntu/x-op/venv/bin/python harvest_run.py (16-20 lanes when under the floor); select per the engagement doctrine with worker/select_latest.py (live windows first; then fresh <2h, likes >=800, sparse replies; workhorse fill when behind quota pace); compose in the midnight register per the skill quality bar (45-110 chars, lowercase, one idea, no dashes, no approval tails, no self-diminishment, fact-gate: skip anything you cannot verify from the post itself); QC hard - cut the merely fine; fire <= max_per_cycle 12 via `bash /home/ubuntu/x-op/run_wave.sh <your-targets.json> --max 12 --gap-min 20 --gap-max 45` (NEVER call fire_driver.py directly; rebuild EVERY url byte-exact from the harvest files by handle, never retype ids). If THROTTLED: stop the batch, set throttle_until = now+15min in state.json; the next cycle must fire a single test reply first, then half pace. After firing, update state.json (day, last_cycle, last_cycle_result, throttle_until) and vault-log the hits. Never: fire from stale lists, retry restricted posts, reply to a handle already replied to today (check the ledger first), or force graveyard-family replies (praise, approval tails, self-comparison, safe observations). Keep output silent - this job delivers locally.